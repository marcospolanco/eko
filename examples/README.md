# Examples

This directory will contain progressive, testable EKO examples, from a minimal empirical claim through policy resolution, workflow instantiation, and a shadow RAG audit.

Each example will pair human-readable source material with valid EKO data, runtime inputs, expected output, and at least one failure or escalation path. No sample implementation has been added yet.

